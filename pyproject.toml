[project]
name = "trovare"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "discord.py>=2.4",
    "fastapi>=0.139.0",
    "python-dotenv>=1.2.2",
    "uvicorn[standard]>=0.50.2",
]
